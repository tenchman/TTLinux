# vim:set syntax=kconfig:
config CONFIG_PACKAGE
  bool "package - A really nice package to do just everything"
  default n
  help
    A really nice Package description and everything about it

    http://www.package.org/
